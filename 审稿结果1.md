# 审稿结果1.md

## 高严重度

1. Topic/File：T03 `resources/seed_data.json`
问题：`Qwen3.5-35B-A3B` 一处写“FP16 约 70GB”，另一处写“910B3 能装”。
原因：910B3 标称 64GB HBM，70GB 权重还未计入 KV、runtime buffer 和框架预留，单卡 FP16 不能直接成立。
建议修改：改为“910B3 需量化、降低上下文/并发或多卡切分后才可能部署”，避免表达成单卡 FP16 可装。

2. Topic/File：T20 `resources/seed_data.json`
问题：`Qwen3.6-27B(dense) 在 910B3 上 FP8 约 27GB 可单卡部署` 过度断言。
原因：权重约 27GB 只说明权重体积，不能推出完整服务可单卡部署；还要计入 262K/128K 上下文 KV/状态、runtime buffer、Ascend kernel 支持和吞吐目标。
建议修改：改为“权重体积进入单卡可评估区间，是否可生产单卡部署取决于上下文长度、KV/状态开销和 vLLM Ascend/CANN 支持”。

3. Topic/File：T21 `resources/seed_data.json`
问题：`1M 上下文 KV Cache 仅为 V3.2 的 10%` 与公开资料中的“2% KV cache figure”口径不一致。
原因：当前引用的公开资料更强调 CSA/HCA、FP8/BF16 RoPE 和压缩率共同带来的约 2% KV figure；“V3.2 的 10%”缺少明确来源和口径。
建议修改：补充来源和对比基准；若不能确认，改成“显著低于 V3.2/传统 MLA 口径，具体比例以技术报告为准”。

4. Topic/File：T01/T09 `resources/seed_data.json`
问题：`MTP 能把并发从 1200 提升到 3000+`、`P90 TPOT 42ms 降到 13-17ms` 被写成确定结果。
原因：这类指标依赖模型、采样参数、接受率、batch、硬件、业务流量分布，不能作为通用技术事实。
建议修改：标注为“内部目标/某次压测观察”，并补充前提条件；通用正文改成“可能显著提升，但需以接受率与端到端 ITL/TPOT 验证”。

5. Topic/File：T10 `resources/seed_data.json`
问题：`Decode 阶段权重减半 → HBM 搬运减半 → 速度接近翻倍` 过度线性化。
原因：量化后的收益还受 dequant、kernel 成熟度、batch 形态、KV 读取、采样和调度影响，尤其 Ascend 上低精度 kernel 不一定同等成熟。
建议修改：改为“理论上降低权重带宽压力，上限接近权重读取占比带来的收益；实际需压测确认”。

## 中严重度

6. Topic/File：T19 `resources/seed_data.json`
问题：`标准 Attention 瓶颈：O(n²) 计算复杂度 + O(n) KV Cache` 容易把 prefill 和 decode 混在一起。
原因：prefill attention 计算近似 O(n²)，decode 单步 attention 更接近 O(n) 读取历史 KV。
建议修改：关键知识点拆成“prefill 计算 O(n²)”和“decode KV 读取/缓存 O(n)”。

7. Topic/File：T01 `resources/seed_data.json`
问题：把 `MTP`、`continuous batching`、`P/D 分离` 都归纳为“提高一次权重搬运的产出”不严谨。
原因：MTP 和 batching 更贴近提高 decode 权重读取产出；P/D 分离主要是隔离 prefill/decode 队列和资源池，降低互相干扰。
建议修改：改成“它们分别从多 token 验证、批内复用、异构资源隔离三个角度改善 decode/服务效率”。

8. Topic/File：T20 `resources/seed_data.json`、`resources/topic-diagrams/t20-qwen-deltanet.svg`
问题：Qwen3.5-397B-A17B 与 Qwen3.6-27B 的结构细节混在一个 Topic。
原因：公开资料显示二者层数、头数、MoE/dense 属性不同；当前正文、key points 和配图都用“3:1”概括，读者可能误以为规格相同。
建议修改：正文增加小表述区分“Qwen3.5-397B-A17B：60 层 MoE；Qwen3.6-27B：64 层 dense”，配图标题改成“3:1 混合范式示意”。

9. Topic/File：T08/T12/T13 `resources/seed_data.json`
问题：多处使用 `Phase 1/Phase 2/Phase 3` 描述 MaaS 平台路线，容易和本 Electron 学习 App 的 Phase 1/2 MVP 边界混淆。
原因：项目规则中的 Phase 1/2 指应用功能边界；内容中的 Phase 1/2 是业务平台阶段。
建议修改：改成“当前平台阶段/下一阶段/远期阶段”，避免读者误解为 App 功能要扩展限流、网关、治理模块。

10. Topic/File：T11 `resources/seed_data.json`
问题：`TTFT 由 Prefill 速度决定` 过窄。
原因：TTFT 还包括网关排队、调度等待、KV 分配、冷启动、首 token 返回链路。
建议修改：改为“TTFT 主要受排队、调度与 prefill 影响”。

11. Topic/File：T12 `resources/seed_data.json`
问题：`one-shot routing：选错无 fallback` 表达过强。
原因：流式响应开始后透明重试确实困难，但请求发出前、失败预算内或非流式场景仍可有有界重试/降级。
建议修改：改成“跨集群隔离使 fallback 成本高，尤其流式响应开始后不宜透明重试”。

12. Topic/File：`src/renderer/lib/markdown.ts`、`src/main/main.ts`
问题：Markdown 允许 `/` 开头链接，生产导航又允许任意 `file:` 协议，存在未来内容误配时的导航边界风险。
原因：当前 seed 未放这类链接，风险不高；但若后续内容加入根路径链接，可能触发非预期 file 导航。
建议修改：Markdown 链接建议只允许 `http(s)`、`mailto`、`#`；或生产环境将 `will-navigate` 收紧到应用入口文件范围。

## 低严重度

13. Topic/File：T13/T16/T17 `resources/topic-diagrams/*.svg`
问题：配图偏“概念卡片”，工程信息密度不足。
原因：T13 只画治理漏斗，T16 只画生命周期环，T17 只画层级栈，缺少关键状态、输入输出、失败路径或指标闭环。
建议修改：补一到两个工程锚点，例如 T13 增加 `KV 水位/输出 token/max_model_len`，T16 增加 `评测门禁/灰度指标/回滚制品`，T17 区分数据面与控制面。

14. Topic/File：`scripts/expand-topic-content.mjs`
问题：生成脚本把大量可变事实硬编码在脚本内，来源文件只列 URL，未逐条绑定断言。
原因：后续模型规格、内部指标或硬件口径变化时，不容易知道哪些正文需要重审。
建议修改：给高风险断言加注释或结构化来源字段，例如 `source_note`、`confidence`、`internal_assumption`。

15. Topic/File：`src/renderer/components/TopicDetailView.tsx`、`src/renderer/lib/markdown.ts`
问题：未发现高风险 Markdown/Electron 安全越界。
原因：正文经 `MarkdownContent` 渲染，`markdown-it` 禁 raw HTML，DOMPurify 禁危险标签/属性，图片限制到 `learning-asset://topic-diagrams/*.svg`，SVG 路径也有主进程白名单。
建议修改：保留现有边界；若未来支持外部图片或内容编辑，必须重新补 XSS、协议和导航测试。
