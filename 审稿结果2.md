# 审稿结果 2 — 昇腾 AI Infra 学习内容升级版

审稿人视角：昇腾 AI Infra / vLLM 推理工程方向。
范围：`resources/seed_data.json`、`resources/topic-diagrams/*.svg`、`scripts/expand-topic-content.mjs`、`src/renderer/components/TopicDetailView.tsx`、`src/renderer/lib/markdown.ts`、`src/renderer/styles.css`。
约定：以下问题按**严重程度**降序，每条包含 Topic/File、问题、原因、建议修改。
本次只读审稿，未对任何源文件做修改；仅落出本审稿结果文件。

---

## 一、高风险（影响事实正确性 / 可能误导读者）

### H1. T20 正文与配图严重不一致：「64 层 = 16 × (3+1)」 ↔ SVG 只画了 4 个块（共 16 层）

- Topic/File：`resources/seed_data.json` T20 `key_points` + `resources/topic-diagrams/t20-qwen-deltanet.svg` + `scripts/expand-topic-content.mjs`(makeDiagram T20)
- 问题：`key_points` 写「Qwen3.6-27B 的 64 层 = **16 个块** × (3 层 Gated DeltaNet + 1 层 Gated Attention)」，但 SVG 用 `[0,1,2,3].map` 只画了 **4 个块**，每块画 4 层（3 + 1），合计 16 层；标题写「3:1 混合层」，没有体现 16 块、64 层。
- 原因：脚本里的 `[0,1,2,3]` 是为视觉简化（避免 16 块挤在 960×420 画布里），但与正文的具体规格直接冲突，读者会得出「64 层模型只有 16 层」的错觉。
- 建议修改：二选一并保持一致——
 - (A) SVG 在标题/副标题里明确「示意 4 个块（实际 ×16）」，且把 `key_points` 里 16 块/64 层放到「示意-真实」对照位；
 - (B) 把正文里的「16 个块 / 64 层」从硬数字改成「若干块、典型 3:1 比例」等定性表述，避免与图不一致。

### H2. T20 模型规格与命名缺乏权威来源支撑（事实风险）

- Topic/File：`resources/seed_data.json` T20、T03、`real_world_connection`、`scripts/expand-topic-content.mjs` 中的 `sourceRows` T20 链接
- 问题：内容反复使用 `Qwen3.5-35B-A3B`、`Qwen3.6-27B(dense)`、`Qwen3.5-397B-A17B`（脚本 `sourceRows` 里）、`64 层 = 16 × (3+1)`、`DeltaNet 层 48 个 V 头/16 个 QK 头, Gated Attention 层 24 个 Q 头/仅 4 个 KV 头`、`27GB FP8 单卡部署` 等高度具体的规格。
- 原因：当前可被外部验证的官方 Qwen MoE 命名以 `Qwen3-235B-A22B` 与 `Qwen3-30B-A3B` 为主；`Qwen3.5-397B-A17B` / `Qwen3.6-27B` / `Qwen3.5-35B-A3B` 这三个型号同时出现，容易把「示意」当成「事实」；而脚本里 `T20` source 又指向 `huggingface.co/Qwen/Qwen3.5-397B-A17B`，与正文 T20 实际描述的 `Qwen3.6-27B` 模型卡并不重合。`CONTENT_SOURCES.md` 又只是「校验记录」，并不是可点开的内置资料；如果链接拉到 404，结论将无法回追。
- 建议修改：
 - (A) 把所有具体型号、层数、头数、显存数字从「事实」措辞改成「示例（写作时刻的公开信息，以官方模型卡为准）」；
 - (B) 同步对齐正文型号与 `sourceRows` 链接（不要正文写 3.6，链接给 3.5）；
 - (C) 在 T20 的「对比与边界」里显式提醒：若 Ascend kernel 未支持 Gated DeltaNet 状态更新，前面所有显存/并发收益都不成立。

### H3. T21 DeepSeek V4 的「4x / 128x / V4-Pro 1.6T / Flash 284B / FP4 QAT」均为高度具体且未引用一手资料

- Topic/File：`resources/seed_data.json` T21 `key_points` / `real_world_connection`、`resources/topic-diagrams/t21-deepseek-csa-hca.svg`
- 问题：把 CSA 「4x 压缩」、HCA 「128x 压缩」、`Lightning Indexer 用 FP4`、`MoE 权重 FP4 QAT`、`V4-Pro(1.6T)/Flash(284B)`、`KV Cache 仅为 V3.2 的 10%` 等数字直接当结论铺到「机制拆解」「key_points」与配图副标题。
- 原因：`sourceRows` 给出的 T21 链接是 `huggingface.co/blog/deepseekv4` 和单条 commit，而非可逐项核对的官方 tech report；这些数字一旦在公开报告里是另一个口径（例如官方用 4× / 8× 压缩档、不同 sliding window 大小），文档会变成「貌似精确的事实陈述」。
- 建议修改：
 - 把 4x / 128x / FP4 / 1.6T / 284B 这些数字替换成「示例倍率（以 DeepSeek 官方技术报告为准）」；
 - 在 T21「机制拆解」开头加一句「以下倍率均为公开技术报告中的示例数值，工程引入前应回到一手报告核对」；
 - SVG 副标题（`4x 压缩 + Top-K 索引` / `128x 压缩 + dense`）同步改为「轻压缩」「重压缩」等定性表述，避免把示意倍率刻死在图上。

### H4. T03 「Qwen3.5-35B-A3B：总参数 35B 但每 token 只激活 3B」当作事实陈述

- Topic/File：`resources/seed_data.json` T03 `key_points` / `real_world_connection`
- 问题：与 H2 同源——`Qwen3.5-35B-A3B` 不是被广泛验证的官方型号；正文「910B3 能装但 910B4 装不下」直接拿这个型号当例子。
- 原因：MoE 概念解释正确，但用一个无法核对的型号作为关键计算锚，读者会把它当成 GTS 已经评估过的真实模型。
- 建议修改：把 T03 的 MoE 例子改回更通用的写法（例如「以一个总参 35B、激活 3B 的 MoE 模型为例（具体型号以模型卡为准）」），或改用确认存在的 `Qwen3-30B-A3B` / `Mixtral-8x7B`。

### H5. T09 关键数字「α=0.83 → 平均接受 4/5」「P90 TPOT 42ms → 13–17ms」「容量 1200 → 3000–3600」缺少口径说明

- Topic/File：`resources/seed_data.json` T09 `key_points` / `real_world_connection`、T08 `real_world_connection`
- 问题：α=0.83 的「平均每次接受 5 token 中的 4 个」并不严谨——投机推理中 expected accept length = (1-α^(K+1))/(1-α)，与「α × K」并不直接相等；P90 TPOT 42→13ms 等价于 ~3× 加速，需要假设 verify 几乎免费、batch 不退化，是「理论上限」而非「上线后已观察值」。
- 原因：
 1. 接受率与平均接受 token 的换算不准；
 2. 数字直接放在 `real_world_connection` 里，看起来像 GTS 实测数据，但来源未注明；
 3. 1200 → 3000–3600 的「容量 ×2.5–3」与 TPOT ×3 的关系也没有上下文（流量画像、batch、上下文长度都没限定）。
- 建议修改：
 - 加注「以下数字为 MTP 理论模拟 / 内部测试集结果，生产 SLO 以现网灰度为准」；
 - 把「α=0.83 → 5 中 4」改为「在 K=N 候选下，期望接受长度 ≈ (1-α^(K+1))/(1-α)，需要按真实采样温度核算」；
 - 在 T09「常见误区」里明确：投机推理在高温采样、长 prefill batch 中可能反而拖累 ITL（这点正文已有，但应与上面的乐观数字放在同一段，避免读者只记住 3×）。

### H6. T08「P90 TPOT 约 42ms，MTP 上线后理论可降到 13–17ms」与 T11 形成数字闭环但未声明假设

- Topic/File：T08 `real_world_connection`、T11 `real_world_connection`
- 问题：T08、T09、T11 三处用同一组数字（42ms、13–17ms、~1200 / ~3000）支撑不同结论，但配比、模型、上下文长度、batch、采样参数等关键前提都没声明。
- 原因：教学性正文 OK，但把它放进「real_world_connection」会被理解为 GTS 现网真实指标，将来一旦数字过期，整组内容都会失真。
- 建议修改：在 `metadata` 增加一个「示例数据基准（写作时间、流量画像、模型）」字段，并在每个用到这组数字的 Topic 顶部 callout 提醒「以下数字为示例基准，非现网最新值」。

---

## 二、中风险（技术不严谨 / 容易误导但可纠正）

### M1. T19 SVG 文字「KV 大」落在内框外、视觉被裁切

- Topic/File：`resources/topic-diagrams/t19-attention-evolution.svg`、`scripts/expand-topic-content.mjs` T19
- 问题：脚本里 MHA 柱高 118，柱顶 y=260，柱底 y=378；下方文字 y=`260+h+20`=398，**超出**了内层面板矩形 (`x=24,y=24,w=912,h=372` → 底边 y=396) 与底部 footnote 行 (y=372)，会与背景色重叠或被裁切，可读性差。
- 原因：高度数组 `[118, 82, 64, 44, 28, 48]` 没有为下方标签预留空间。
- 建议修改：把柱体高度上限缩到 ≤ 100，或把标签 y 计算从 `260+h+20` 改为 ≤ 360，并把内层 panel 的 y 也下移；同时让「主线…」文字和「表达力 vs 成本」box 不要再压住柱体顶端。

### M2. T06 「Prefix Caching：Radix Tree 前缀匹配」不严谨

- Topic/File：`resources/seed_data.json` T06 `key_points`
- 问题：vLLM 的 Prefix Caching 主线实现是基于 **block hash** 的复用；Radix Tree 是 SGLang/RadixAttention 系的常见说法。把两者混为一体容易让读者以为 vLLM 内部就是 Radix Tree。
- 原因：术语错位。
- 建议修改：改为「Prefix Caching：相同前缀的 KV block 通过哈希复用（vLLM）或前缀树匹配（SGLang RadixAttention）」。

### M3. T11 「吞吐 = 时间窗口内输出 token / 资源」公式右侧不规范

- Topic/File：`resources/seed_data.json` T11 `body_md`「推导示例」
- 问题：吞吐量纲应是 `tokens / time`，正文写成 `…/资源`，把两个不同维度混到一个公式里。
- 原因：教学性比喻被当公式直接写死，新手会理解成「除以卡数」，但卡数本应是规模化维度而非分母。
- 建议修改：写成「吞吐 = 输出 token 数 / 时间窗口」，并另起一句「按资源（卡数、模型实例数）归一可得**单卡 / 单实例吞吐**，便于跨集群比较」。

### M4. T01 「单 token decode 需要读取的权重量级」措辞模糊，缺少 GQA/张量并行修正

- Topic/File：`resources/seed_data.json` T01 `body_md`「推导示例」
- 问题：原文先估「单 token decode 读取权重量」再估 FLOPs；但在张量并行 / MoE / KV 也参与读带宽的现实下，「权重量」单一变量不足以解释 Roofline 的 Bytes 项。
- 原因：用「权重」指代所有需读 bytes，会让读者忽视 KV 读、激活读、通信带宽这几项重要分子。
- 建议修改：把「权重量」改为「单 token decode 在该层需要读的权重 + KV + 激活」并在括号里补「张量并行 / MoE / GQA 会改变这些比例」。

### M5. T08 文中混用 `HCCS` 与 `HCCL`，未澄清「硬件互联 vs 集合通信库」

- Topic/File：`resources/seed_data.json` T08 `key_points` + `body_md` 「昇腾落地点」
- 问题：HCCS 是 Huawei Cache Coherent System（硬件高速互联），HCCL 是 Huawei Collective Communication Library（软件库）；正文「KV 传输 HCCS 高速互联」+「HCCS/HCCL 带宽」两处来回切，会让读者把它们当作同义词。
- 原因：术语层级混淆。
- 建议修改：在 T08「机制拆解」末尾加一行注解，明确 HCCS = 硬件层 / HCCL = 软件层；后续两处只保留各自正确的一个名词。

### M6. T10 「Decode 阶段权重减半 → 速度接近翻倍」过度乐观

- Topic/File：`resources/seed_data.json` T10 `body_md`、T10 SVG `box('显存/带宽下降', '更容易放下')`
- 问题：只在 decode 完全 weight-bound、且没有低位 kernel 退化的前提下才接近 2×；KV 读、激活读、采样、调度都没减半，常见经验值是 1.3–1.7×。
- 原因：过度断言。
- 建议修改：把「速度接近翻倍」改为「decode 读权重带宽显著下降（实测速度提升通常 < 2×，并依赖 kernel 成熟度）」。

### M7. T11 「ITL/TPOT」等价混用

- Topic/File：`resources/seed_data.json` T11 `body_md`、`key_points`
- 问题：ITL（Inter-Token Latency，相邻输出 token 时间差）与 TPOT（Time Per Output Token，平均每输出 token 时延）口径并不完全相同；正文并列使用且没有区分。
- 原因：业内常见混用，但这是 metric 教学篇，恰恰应该把口径讲清楚。
- 建议修改：在 T11 「机制拆解」补一句「ITL 与 TPOT 在大多数文献里近义，但 ITL 强调相邻 token 时间分布，TPOT 强调平均；用在 SLO 时建议固定一个口径」。

### M8. `expand-topic-content.mjs` 「专家读图」徽章与 footnote 重复语义

- Topic/File：`scripts/expand-topic-content.mjs` `diagramShell`
- 问题：每张 SVG 右上角都贴了「专家读图：问题→机制→决策」徽章，左下又把 footnote 强行替换为「专家读图：…」（`footnote.replace(/^读图：/, '专家读图：')`），结果同一张图同时出现两次「专家读图」措辞，且「问题→机制→决策」与具体 footnote 没有视觉/语义连接。
- 原因：模板修饰过度。
- 建议修改：保留一个即可；右上徽章留下，左下保留原 `读图：…` 措辞，让两者职责分开。

### M9. T13 「`Redis 做分布式状态` / 令牌桶+滑动窗口混合限流」未在文字里出现，但配图分四层（入口→配额→优先级→降级），层与层之间的覆盖关系不够清晰

- Topic/File：`resources/topic-diagrams/t13-traffic-governance.svg`、T13 `body_md`
- 问题：四层「水池」式叠落很直观，但没有体现「同一个请求会同时经过哪些治理动作」；读者容易把它误读成顺序流水线，而不是策略组合。
- 原因：图与文字略错位。
- 建议修改：在配图加一条贯穿四层的请求路径线，明确「不是层层串联，而是同一请求按需触发不同策略」。

---

## 三、低风险（表述、版式、一致性）

### L1. T17 SVG 五条层级条尾部「层叠收窄」是装饰，但「Model Registry / Observability」放进同一行，与正文中「Model Registry」「Observability」分别独立的描述不一致

- Topic/File：`resources/topic-diagrams/t17-maas-architecture.svg`
- 建议：拆成两行或在副标题里区分「资产层 / 观测层」。

### L2. T16 SVG 状态机箭头方向看起来像循环，但箭头连线 `arrow(370,292,314,232)`（Monitor → Rollback）会让读者以为「监控之后总要回滚」

- Topic/File：`resources/topic-diagrams/t16-model-lifecycle.svg`
- 建议：把 Monitor → Rollback 的箭头改为虚线 + 标记「异常时」，或直接去掉这条边、让 Rollback 改由「Canary 失败」单向触发。

### L3. T05 SVG 五层菱形堆叠把「应用框架」画在最底（y 最大）但实际上是最高层

- Topic/File：`resources/topic-diagrams/t05-cann-npu-stack.svg`
- 问题：文本数组 `['AI Core: Cube / Vector / Scalar', 'Runtime', 'TBE / Ascend C 自研算子', 'GE 图优化', 'vLLM / PyTorch / 应用']` 是「自底向上」，画到 SVG 上形成倒金字塔，与读者一般「应用在顶」的预期相反，但 catch 又写「应用框架像汽车」让人以为最顶=应用。
- 建议：在副标题加「自底向上」字样，或反向数组让应用框架在视觉最顶端。

### L4. T17 / T18 SVG 全部用「层叠收窄矩形」，缺乏区分

- Topic/File：`resources/topic-diagrams/t17-maas-architecture.svg`、`t18-hardware-strategy.svg`
- 建议：T18 已是放射状（OK），T17 仍为层叠；只是审美上略单调，可保持。

### L5. `body_md` 「推导示例」段落把 `addon.derivation` 和 `derivationSteps` 用一个空行分隔，渲染上是「正文段落 + 有序列表」紧贴，没有 callout 强调

- Topic/File：`scripts/expand-topic-content.mjs` `topicBody`
- 建议：在 `derivationSteps` 之前用「**推导步骤：**」加粗副标题，避免列表看起来像与正文割裂的脚注。

### L6. `expand-topic-content.mjs` 在主流程里 `topic.id === 'T04'` 时单独覆盖 `topic.key_points`，但其他 Topic 的 key_points 来源仍是 seed 原值

- Topic/File：`scripts/expand-topic-content.mjs` 末尾循环
- 问题：脚本意图是 T04 用更专家化的 key_points 覆盖原值，但只有 T04 这么干，其他 Topic 的 key_points 仍依赖 seed 已有写法。这会让 T04 与其他 Topic 的「key_points 风格」明显不一致；也容易让后续维护者在两处（seed + 脚本）对同一字段产生分叉。
- 建议：要么把所有 Topic 的 key_points 都集中到脚本（`expertAddons` 里统一管理），要么把 T04 的覆盖逻辑改成在 seed 里直接写好；不要让一个字段的来源跨两个文件。

### L7. `seed_data.json` 中「`MiniMax 2.5` / `MiniMax M2.5`」两种写法都出现

- Topic/File：T04 / T10 / T16 / T19 等
- 建议：统一为同一个写法，避免读者以为是两款模型。

### L8. CSS 没有为 `.markdown-body img` 加 `loading="lazy"` 或骨架，但 Markdown 里 21 张 SVG 全部内联

- Topic/File：`src/renderer/styles.css`、`src/renderer/lib/markdown.ts`
- 问题：21 张 SVG 通过 `learning-asset://` 一次性渲染，单 Topic 切换时不会有压力（每屏 1 张），但若日后扩展到一篇多图，会有 layout shift。
- 建议：MVP 不做改动；后续考虑给 `img` 加固定 `width`/`height` 占位。

---

## 四、Phase 1 / 2 MVP 边界与 Electron / Markdown 安全边界检查

> 这一节是「**审过的事实**」，不是问题。除非另说，没有发现高风险违反。

### S1. 新增 `learning-asset://` 自定义协议没有越过 MVP 安全边界 — **OK**

- `src/main/main.ts` 注册为 `privileged + secure + supportFetchAPI`，并通过 `protocol.handle('learning-asset', ...)` 走 `getTopicDiagramPath()` 解析。
- `src/main/paths.ts` 用严格正则 `/^learning-asset:\/\/topic-diagrams\/[a-z0-9-]+\.svg$/i` 限制文件名（只允许小写字母/数字/连字符 + `.svg`），并用 `path.relative` 防止 `..` 逃逸；`test/paths.test.ts` 已覆盖 `../secret.svg`、`other/...`、`https://...`、`.png` 四个反例。
- `src/main/security.ts` CSP 正确加入 `img-src 'self' data: learning-asset:`，未放宽 `script-src` / `style-src`，与 `CLAUDE.md` 的安全基线一致。
- `BrowserWindow` 仍保持 `contextIsolation:true` / `sandbox:true` / `nodeIntegration:false` / `webSecurity:true`（在 `test/electronSecurity.test.ts` 里有断言）。

### S2. Markdown XSS 防线 — **OK**

- `src/renderer/lib/markdown.ts` 维持 `markdown-it html:false` + `DOMPurify` + `ALLOWED_URI_REGEXP` 白名单 + `afterSanitizeAttributes` 双重 hook：
 - 链接 scheme 仅 `http(s) | mailto | # | /`；
 - 图片 src 必须命中 `LEARNING_ASSET_IMAGE` 正则，否则 `removeAttribute('src')`；
 - 外链强制 `rel=noopener noreferrer target=_blank`。
- `enhanceCallouts` 在 sanitize **之后**用 `template.innerHTML` 解析，所有 DOM 操作只动 `textContent`/`createTextNode`/`createElement('strong')`，不会引入新的 raw HTML，安全。
- `test/markdown.test.ts:72` 已覆盖「只允许 bundled 图、`../secret.svg` 会被剥掉」的反例。

### S3. Phase 1 / 2 MVP 边界 — **OK，无新功能扩展**

- 上次范围（DAG + Markdown）保持不变；本次升级只是把 21 个 Topic 的 `body_md` 写得更专家化、加了 21 张内置 SVG 图。
- 没有引入新 IPC、没有引入新存储、没有引入考试 / CRUD / 导入导出。
- `forge.config.ts` 的 `extraResource` 已加入 `resources/topic-diagrams`，与「打包资源从 `process.resourcesPath` 解析」基线一致。

### S4. 测试覆盖 — **OK**

- `test/paths.test.ts` 覆盖 `resolveTopicDiagramPath` 正向 + 4 个反向用例；
- `test/markdown.test.ts` 覆盖图片白名单；
- `test/learningStore.test.ts:135-144` 在加载 seed 时校验每个 Topic 的 SVG 文件实际存在；
- `test/electronSecurity.test.ts:29` 断言 CSP 中包含 `learning-asset:`；
- 这一组保护使「未来再加一个 Topic 但忘了同步 SVG / 图链接拼错 / CSP 漏改」会立刻在 CI 上爆出来。

### S5. 仅有的边界提示（非问题）

- `expand-topic-content.mjs` 是离线脚本，跑完后产物会改写 seed_data.json；脚本本身不会进入运行时，不影响 Electron 安全面。
- `CONTENT_SOURCES.md`（脚本会生成）只是离线参考资料清单，与运行时数据无耦合，符合「不在运行时访问外网」的边界。
- Phase 1 / 2 边界上没有发现高风险违反。

---

## 五、修复优先级建议（给后续迭代用）

1. **先修 H1 + H2 + H4**（T20 / T03 模型规格与 SVG 不一致），影响最严重；
2. **再修 H3**（T21 倍率/型号），同时下调 H5 / H6 数字断言措辞；
3. **后修 M1**（T19 SVG 越界）、**M2 / M3**（vLLM Prefix Caching、吞吐公式）、**M5**（HCCS/HCCL）、**M6**（量化乐观）、**M7**（ITL/TPOT 区分）；
4. 最后扫一遍 L 系列（视觉一致性、命名一致性、key_points 来源一致性）；
5. 安全 / MVP 边界部分**无需改动**，继续保持现状。

---

> 本审稿结果仅依据本次提交的源码与 seed 内容做交叉验证，不依赖外部网络抓取，也未对仓库内任何文件做出修改。文中点出的型号、倍率、TPOT 数字若属于「未来确实会公开」的真实规格，请在引入时回到一手模型卡 / 技术报告核对一次。
